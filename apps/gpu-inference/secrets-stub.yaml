# Secrets stub for gpu-inference namespace.
# Real values applied out-of-band by reroll script; this file is documentation only.
#
# Secrets in this namespace:
#   - gitea-registry   dockerconfigjson   (gitea registry pull creds for 192.168.100.206)
---
apiVersion: v1
kind: Secret
metadata:
  name: gitea-registry
  namespace: gpu-inference
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: "" # PLACEHOLDER — restored from backup / created via kubectl create secret docker-registry
